import { accBroadcastingNetworkLib } from './acc-broadcasting-network-lib';

describe('accBroadcastingNetworkLib', () => {
  it('should work', () => {
    expect(accBroadcastingNetworkLib()).toEqual('acc-broadcasting-network-lib');
  });
});
